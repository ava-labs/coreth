# This file configures github.com/golangci/golangci-lint.

run:
  timeout: 10m

issues:
  new: true

linters:
  disable-all: true
  enable:
    - goimports
    - gosimple
    - govet
    - ineffassign
    - misspell
    - unconvert
    - unused
    - whitespace
