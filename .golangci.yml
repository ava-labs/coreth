# This file configures github.com/golangci/golangci-lint.

run:
  timeout: 10m

linters:
  enable:
    - errorlint
    - goimports
    - ineffassign
    - misspell
    - unconvert
    - whitespace

linters-settings:
  errorlint:
    errorf: false

issues:
  exclude-rules:
    - path-except: "(plugin\\/.+\\.go|.+_ext\\.go)"
      linters:
        - errcheck
        - errorlint
        - staticcheck
